# 2026-04 History

这份文件承接逐日详细变更。

从 `2026-04-01` 开始：

- `AGENTS.md` 和 `docs/status_snapshot.md` 不再堆逐日 log。
- 默认入口文档只保留规则、成熟度、主线 backlog 和短摘要。
- 详细逐日变更统一归档到这里。

## 2026-04-01

### 情报链 v1 收口

- `event_digest -> editor_payload -> client_report` 共享情报链已在 mature 主链收口：
  - 多条相关情报
  - 信号类型 / 信号强弱
  - 情报属性 / 来源层级
  - client-safe 空情报窗口表达
- `scan / stock_analysis / stock_pick / etf_pick / fund_pick / briefing` 已统一清掉大部分内部 miss 诊断语。
- 结构化披露会优先补官方查询入口，不再大量掉成纯文本。

### 晨报改成情报总台

- `briefing` 已从“单条 lead item”改成“广覆盖情报板”：
  - 轻量 RSS
  - 广覆盖 RSS
  - `Tushare market intelligence`
  - query-group 搜索回填
- 首页与 `今日情报看板` 会优先前置多条可链接外部情报，而不是只给内部主线校验句。

### Observe-only 信息密度补回

- `stock_pick` 的 observe-only 分支已补回：
  - `第二批：继续跟踪`
  - `第二批：低门槛 / 观察替代`
  - `低门槛继续跟踪`
  - `代表样本复核卡`
- 目标是恢复 `03-20` 那批最有价值的信息密度，但不回退成超长 appendix。

### 单标的情报表达收口

- `stock_analysis` 现在会把“旧闻回放”明确降成 `历史基线`，不再把旧公告/旧分红误写成新增催化。
- 首页动作层已收成“观察边界”，不再在 observe-only 稿件里前置精确仓位、止损或执行模板。

### 今天的 final

`2026-04-01` 已落盘的正式稿：

- `stock_pick`
- `etf_pick`
- `fund_pick`
- `scan_563360`
- `stock_analysis_600313`
- `daily_briefing`

对应外审也都已补成 PASS。

## 2026-04-02 / 2026-04-03

### Tushare 10000 分主链继续收口

- 股票链的 `broker_recommend` 已从共享 collector 进入正式稿可见证据，不再只停在底层结构化快照里。
- `上证e互动 / 深证互动易` 已接进共享 `news` collector，并下沉到 `scan / stock_analysis / stock_pick / briefing`；当前是否在 final 中出现，取决于样本当天是否真实命中到可展示的 IR 证据。
- ETF phase 2 已继续收口：
  - `fund_factor_pro` 已进入 `fund_profile / opportunity_engine / etf_pick / scan / client_report / opportunity_report`
  - `etf_share_size` 已默认拉近 7 个开盘日做两点变化
  - `etf_pick / ETF scan` 已能把 `跟踪指数 + 份额变化 + 场内基金技术状态 + 外部情报状态` 写进正文，不再只挂在信息表里

### 正式稿可见性与版式修复

- `briefing / stock_pick` 的正式稿已把 `卖方共识专题` 抬到可见证据层，不再长期被 generic 框架行挤掉。
- `etf_pick` 的 `场内基金技术状态 / 申赎线索 / 规模线索` 已进入“为什么先看它”，不再只埋在 `ETF专用信息` 表里。
- `stock_analysis` 的 PDF 首页版式已单独修正：首页判断层与 `## 一句话结论` 之间现在会强制打印分页，避免 PDF 首页把执行摘要挤成一团。

### 晨报盘面语义修正

- `briefing` 的 `A股行业走强 / A股概念领涨` 已补正负过滤：负涨幅方向不会再被写成 `走强 / 领涨`。
- 共享 `market_event_rows` 也同步收口：若盘面板块跌幅为负，标题会按 `承压` 处理，结论也会转成谨慎/走弱口径，不再把负涨幅行误写成利多。

### 第二阶段 backlog 扩容

- 对照 Tushare `10000` 分权限页和仓库当前 `_ts_call`，计划文档已补出“已解锁、未接、也未排进主计划”的第二阶段 backlog：
  - `daily_info / sz_daily_info`
  - `stk_factor_pro`
  - `stk_surv`
  - `stk_ah_comparison`
  - `fx_basic / fx_daily`
  - `fund_sales_ratio`
  - `cb_basic / cb_daily / cb_factor_pro`
  - `sge_basic / sge_daily`
