# 2026-03 Change Log

这份文件是按需阅读的历史归档，不是默认入口。

只有在需要追某个判断为什么变、某个合同是什么时候收口、或者某个 backlog 为什么迁移时再读。

## 2026-03-15

### 基金画像和 review kit

- 基金画像链已把 Tushare `fund_manager / fund_company / fund_div / fund_portfolio` 接进 `fund_profile`
- 外审能力已补成可迁移 kit：
  - `docs/review_kit/README.md`
  - `docs/review_kit/review_record_template.md`
  - `docs/review_kit/review_ledger_schema.md`
  - `docs/review_kit/migration_checklist.md`
- `review_ledger` 和 `review_audit` 已落地第一版

## 2026-03-16

### 客户成稿和导出链收口

- `client_export` 的 HTML 导出会把本地图片嵌成 `data:` URI`
- 全局 HTML/PDF 主题已升级
- `scan / stock_analysis` 的客户 final 改成“内部详细分析结构 + 客户友好章节名”

### final / manifest / review 刷新

- `stock_pick / fund_pick / etf_pick / briefing` 的 `2026-03-16` final、external review、manifest 已重刷
- manifest 现在统一带 `factor_contract`
- `review_audit` 对 `structured-round` 审计为 `0 active findings`

### 其它合同收口

- `briefing` 的 A 股观察池开始复用成熟个股发现链
- 阶段 J（强因子工程）按 `v1 已收口` 切出主开发主线
- `trade_handoff` 口径已统一
- `history_fallback` 命中时图表层直接不出图
- 证据时点与来源会显式写出 `行情来源`

## 2026-03-17

### 强因子链变成真正双向

- `催化 / 相对强弱 / 筹码结构 / 季节日历` 已补成更均衡的双向表达
- `fundamental / macro` 也已开始对高估值、弱增长、低 ROE、景气/信用逆风等形成轻中度拖累
- `factor_meta` 已能覆盖这些核心因子的 `factor_id / factor_meta`

### K 线形态与中国日线主链收紧

- `technical.py` 已补入 `三内升/三内降`、`看涨/看跌母子线`、`平顶/平底镊子线`、`上吊线`
- 这些形态已接入技术评分、动作建议和正文叙事
- A 股/ETF 的日线抓取新增了 Tushare 主链重试

### 功能开发快路径固化

- 默认节奏已收成：
  - `patch-level`
  - `family-level`
  - `stage-level`
- 目的是降低每个小 patch 的 today final / 外审 / token 成本

## 2026-03-18

### ETF / 基金发现链提速

- `build_market_context` 改成并行预热独立段
- `discover_opportunities / discover_fund_opportunities / etf_pick watchlist fallback` 改成有界并发
- 基金画像共享表补了进程级缓存
- 7 只 ETF 小样本实测，总墙钟大约从 `62s` 降到 `39-40s`

## 2026-03-19

### `briefing market` 落地

- `briefing` 新增 `market` 模式，正式把市场全景和 A 股观察池收成“全市场行情简报”
- 主线 taxonomy 扩容，新增 `黄金避险 / 红利银行防守 / 宽基修复 / 电网公用事业`
- A 股观察池行业分布会回灌到主线评分

### A 股观察池复用成熟链路

- `briefing market` 的 A 股观察池开始复用 `briefing` 主链已有的市场上下文
- `discover_stock_opportunities` 已补成受控并发
- 瓶颈从“重复拉上游上下文”收缩到“A 股候选本身的深分析”

## 2026-03-20

### 客户稿先给结论，再给解释

- `scan / stock_analysis / stock_pick / etf_pick / fund_pick` 的 major section 会统一补一行 `先看结论`
- `观察 / 回避` 口径动作区新增 `触发买点条件`
- 观察稿还会补 `关键盯盘价位`

### `stock_pick` 客户详细稿分层

- `正式推荐` 继续保留完整八维、催化、硬检查和风险拆解
- `看好但暂不推荐 / 观察为主` 压成短版卡片
- 观察名单同时保留 `1` 只代表样本详细拆解，满足 final 门禁

### 个股评级轻量校准

- `基本面/催化主导` 路径把技术门槛从 `40` 放宽到 `35`
- `趋势延续` 路径把门槛从 `tech>=60 / relative>=70 / risk>=55 / catalyst>=35` 放宽到 `tech>=55 / relative>=65 / risk>=45 / catalyst>=25`

### 推荐口径去“只会观察”

- `stock_pick / etf_pick` 默认给出 `短线优先 + 中线优先`
- A 股个股推荐拆成 `核心主线` 和 `低门槛可执行 + 关联ETF平替`
- `etf_pick` payload 会拆 `short_term / medium_term`

### ETF / 基金推荐合同补强

- 低覆盖率稿件会真正退成 `摘要观察稿`
- `回避 / 观察` 结论下会裁掉完整交易动作表
- ETF / 基金维度表的 `基本面` 改按 `产品质量/基本面代理` 披露
- 正式报告外审合同补成双层：`rich prompt 结构化审稿 + 零提示发散审`
- `etf_pick / fund_pick` 的客户稿区分 `配置视角` 和 `交易视角`
- ETF/基金催化面新增 `产品/跟踪方向催化`
- 动作区新增 `建议买入区间` 和 `建议减仓区间`

### 个股推荐校准

- `macro_reverse` 不再机械把所有原本能到 `⭐⭐⭐` 的个股统一压回 `⭐⭐`
- 补了一条更克制的“趋势延续”晋级路径
- `etf_pick / fund_pick` 的盘中全市场发现 final 统一标成 `盘中快照成稿`

## 2026-03-21

### `strategy` benchmark fixture v1

- `predict / replay` 现在会显式写出 `benchmark_fixture`
- fixture 会披露资产窗口、基准窗口、overlap 行数、`as_of` 是否对齐、未来 `20d` 验证窗是否 ready
- `validate / experiment` 摘要也会汇总这层 fixture
- 如果 benchmark overlap 不足或 `as_of` 未对齐，主预测会退回 `no_prediction`

### proxy signals 进入 `briefing / research / retrospect`

- `briefing` 会在正文、payload 和 final manifest 里正式带出 `proxy_contract`
- `research` 新增统一的 `代理信号与限制` 段落
- `portfolio review / retrospect` 会把交易当时保存下来的 `proxy_contract` 写进 `decision_snapshot`
- 旧交易如果没有历史代理快照，不会伪造 point-in-time 代理判断

### proxy signals 进入 pick 正式合同

- `stock_pick / etf_pick / fund_pick` 的客户稿现在显式写出 `市场风格代理 / 情绪代理`
- pick final 的 manifest 已补进 `proxy_contract`
- `review_audit` 开始审这层合同

## 2026-03-22

### 观察稿口径继续收紧

- `scan / stock_pick / etf_pick / fund_pick` 的客户稿开头统一补了高密度 `执行摘要 / 今日动作摘要`，先把 `当前建议 / 适用周期 / 空仓怎么做 / 持仓怎么做 / 仓位与主要利空利好` 说清，再往下展开正文。
- `scan` 的观察型成稿开始固定写 `为什么还不升级 / 升级条件 / 关键盯盘价位`，`催化面` 也改成 `直接催化 / 舆情环境` 两层，不再把纯热度误写成直接催化。
- `stock_pick / etf_pick / fund_pick` 也复用了这套桥接句与正式动作阈值合同：会先明确“方向还在，但还不是正式动作”，再解释为什么仍停留在 `观察为主 / 暂不出手`。

### `strategy` fixture 和 gate 第一轮成形

- `strategy` 在这一天集中补了：
  - `lag / visibility fixture`
  - `overlap fixture`
  - `promotion / rollback gate`
  - `out-of-sample / chronological cohort validate`
  - `cross-sectional validate`
  - `multi-symbol replay / experiment`
- `validate / experiment` 的正式稿也开始固定前置 `这套策略是什么 / 这次到底看出来什么 / 执行摘要`，不再一上来就是账本或调试表。

### 正式稿外审与交付规则继续收口

- 正式 `final` 的外审已正式拆成 `结构审 + 发散审` 两个 reviewer pass，不再允许一个 reviewer 同时兼任两层。
- `strategy validate / experiment` 开始承认正式 `client-final` 导出链，默认会产出 `markdown + html + pdf + release_manifest`，缺 `__external_review.md` 时会自动落 scaffold，而不是静默跳过。

## 2026-03-23

### 共享 final runner 接管成熟命令

- `scan / stock_analysis / stock_pick / etf_pick / fund_pick / briefing` 的 `client-final` 导出开始统一走共享 final runner。
- 这条 runner 会复用：
  - `detail 写盘`
  - `release_check`
  - `report_guard`
  - `markdown/html/pdf/manifest`
- review scaffold 和 auto-close 也开始共享处理：缺 `__external_review.md` 时会自动补首轮 scaffold，只差 round-based PASS 闭环时会自动补收敛记录。

### Tushare timeout 与 HTML 表格合同修正

- `BaseCollector._tushare_pro()` 开始统一承认 repo 级 timeout，不再只由个别命令层包线程超时。
- client HTML 表格的 markdown `\\|` 转义解析也被修掉，`证据时点与来源` 里的 `数据覆盖` 不再错误拆列。

### pick 口径继续真实化

- `etf_pick / fund_pick` 的正式交付如果排第一的标的仍然只是 `观察 / 暂不出手 / 持有优于追高`，会直接退成观察稿，不再把观察动作包装成推荐。
- ETF / 基金 pick 的 `筹码结构` 也开始正式降成辅助项，不再在客户稿里渲染成会干扰判断的硬分数。

## 2026-03-24

### 单标的催化层边界补强

- A 股单标的发散审把一个共享边界正式收口：
  - `宏观背景`
  - `主题逻辑`
  - `个股直接催化`
  - `正式动作触发`
  这四层必须显式拆开，不能再把宏观晨报或行业早报冒充成个股级直接催化。
- processor 侧也同步锁住：只有命中个股级 leader items 时，`龙头公告/业绩` 才能加分。

### 正式导出与外审合同统一

- `f6c365d Unify final delivery and tighten review contracts` 这一批提交对应的是把正式交付和 review contract 收成共享层，减少各命令各写一套导出/审稿逻辑。

## 2026-03-25

### editor payload / editor prompt 侧车进入成熟链

- `scan / stock_analysis / stock_pick / etf_pick / fund_pick / briefing / strategy / portfolio review(retrospect)` 的 `client-final` 开始统一落：
  - `editor_payload.json`
  - `editor_prompt.md`
- 其中 `scan / stock_analysis / stock_pick / etf_pick / fund_pick / briefing` 的首页判断层也开始直接进入正式稿正文，不再只有少数命令有 editor 输入。

### AI editor / subagent 阶段定型

- `stock_analysis / etf_pick` 已接入第一版“首页判断层”共享 payload，正式稿会固定前置：
  - `一句话阶段判断`
  - `以下是关键判断`
  - 编号判断
  - 结论
- editor 的默认执行方式也正式改成 `独立 subagent`，主执行者先出 `editor_payload / editor_prompt`，再让 editor agent 只改首页判断层，之后才进入结构审和发散审。

### 晨报与 ETF final 再稳定一轮

- `2270f97 Stabilize today briefing and ETF final delivery` 对应的是当天对 `briefing / etf_pick` 的稳定化，重点是让正式稿链条更稳，不再频繁卡在导出或 final 前最后一公里。

## 2026-03-26

### `theme playbook v1` 大规模扩容

- 第一批高频主题认知卡正式落到 `docs/theme_playbooks/`，覆盖：
  - 半导体与先进封装
  - AI算力
  - 固态电池 / 光伏 / 储能 / 锂电 / 电网
  - 机器人 / 自动驾驶 / 低空经济
  - 资源 / 黄金 / 煤炭
  - 创新药 / BD出海 / 临床催化 / CRO
  - 消费复苏 / 白酒 / 家电 / 免税文旅
  - 地产链 / 高股息 / 中特估 / 数据要素 / 政策驱动
  - 华为 / 苹果 / 英伟达 / 特斯拉 / 泛事件产业链
- `stock_analysis / etf_pick / stock_pick / fund_pick / briefing` 的首页判断层也同步升级到 `thesis-first v2`，不再只是规则摘要。

### 催化面开始显式区分“没有”与“漏抓”

- live 模式下，如果主题本身按常识应有持续新闻流、但动态检索没抓到标题，`catalyst` 会退成 `待 AI 联网复核`，而不是直接记成 `0 分`。
- 命中 `suspected_search_gap` 的正式稿会统一额外落：
  - `catalyst_web_review_payload.json`
  - `catalyst_web_review_prompt.md`
  - `catalyst_web_review.md`
- `catalyst_web_review.md` 也进入 final 硬门：若还是 `待补` 模板，`report_guard` 直接拦。

## 2026-03-27

### 观察稿首页与主题识别再收紧

- `scan / stock_pick / etf_pick / fund_pick` 的观察稿首页现在会前置 `今天没有有效动作信号` 声明，但后文不再因此短稿化，图表、因子、证据和观察点位仍保留。
- 主题识别新增“首要对象优先”偏置，避免标题主对象被上下文中的次级主题抢走。

### `theme_playbook` conflict guard v1

- `theme_playbook` 新增第一版 `conflict guard`，对易混组如果证据没明显拉开，会直接退回 `sector fallback`，并把“暂不要硬落单一细主题”的原因写进首页与 editor prompt。
- 行业层 fallback 也补了 `sector -> subtheme bridge`，让行业层稿件还能显式带出建议下钻方向。

## 2026-03-28

### `conflict guard` 继续扩组

- 冲突保护继续扩到：
  - 华为链 / 苹果链 / 英伟达链 / 事件产业链
  - 消费复苏 / 白酒 / 家电 / 免税文旅 / 大众消费
  - 政策驱动 / 数据要素 / 中特估
- 对消费 ETF、政策主题 ETF 这类泛主题标的，若身份层与上下文子主题打架，会退回行业层并显式说明冲突原因。

### 冲突诊断进入候选解释层

- 除了 `theme_lines` 和 `editor_prompt`，`homepage.micro_lines` 也开始直接写“哪几条线还在打架、当前先按行业层观察”。

### `strategy` 多标的 validate / experiment 落地正式稿

- `strategy` 的多标的 validate / experiment 已真实进入正式稿，但定位开始明确转向后台置信度层，而不是扩成新的独立大产品。

## 2026-03-29

### 连续研究主线开始成形

- 连续研究从“有状态”推进到“有动作”：
  - `thesis_state`
  - `What Changed`
  - review queue
  - review action
  - run history
- 默认目标已经不再只是“单次出稿”，而是支持旧稿复查、观点升级/降级和复查队列。

## 2026-03-31

### mature 主链清理内部诊断语

- `scan / etf_pick / fund_pick / stock_analysis / stock_pick` 的客户稿开始系统清掉：
  - `未命中...`
  - `覆盖率摘要`
  - `当前可前置的一手情报有限`
  这类内部 miss 诊断语。
- 空情报窗口统一改成 client-safe 表达，旧闻回放、空覆盖和弱覆盖开始按“边界/历史基线”写，而不是把内部诊断直接漏给客户。
