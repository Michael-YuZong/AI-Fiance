# /lookup

## 输入

- ETF 中文名称
- 行业 / 主题关键词
- 或包含“代码 / 编号”的自然语言问题

## 执行流程

1. 先查本地 `config/asset_aliases.yaml`
2. 再补充 watchlist 里的名称和代码别名
3. 本地没命中时，再尝试实时 ETF 列表搜索

## 输出

- 最佳匹配标的
- 候选代码列表
- 后续可直接执行的 `scan` / `assistant` 示例

## 适用场景

- 用户只知道中文名称，不知道代码
- 用户说“有色金属ETF代码是多少”
- 自然语言入口需要先把主题名解析成标的代码
